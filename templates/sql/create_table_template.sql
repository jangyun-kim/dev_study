-- Template for creating tables

-- ===================================================
--                  Fill your code
-- ===================================================

CREATE TABLE IF NOT EXISTS sample_table (
    id INT,
    col1 VARCHAR(255),
    created_at TIMESTAMP
);
